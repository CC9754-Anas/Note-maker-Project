<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini_Project</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 p-10 text-white">
        <div class="flex justify-between">
            <h1 class="text-3xl font-medium">Edit the post:</h1>
            <a href="/logout" class="bg-red-500 px-3 py-2 rounded-md inline-block mb-5 ">LOGOUT</a>
        </div>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="block w-1/3 text-zinc-400 rounded-md resize-none bg-zinc-800 outline-none p-3 border-2 border-zinc-700 mb-3" name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 w-fit cursor-pointer rounded-md outline-none text-black font-medium bg-yellow-500 mb-2" type="submit" value="Update Post">
        </form>
    </div>
</body>
</html>